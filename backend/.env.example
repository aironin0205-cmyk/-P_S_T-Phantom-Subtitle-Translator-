# ================================================================
# ===         APPLICATION ENVIRONMENT CONFIGURATION            ===
# ================================================================
# Copy this file to '.env' for local development and fill in the values.
# The .env file is ignored by Git and should NEVER be committed.

#---------------------------------
# SECTION 1: CORE APPLICATION
#---------------------------------
# The environment the application is running in.
# Valid options: "development", "production", "test"
NODE_ENV="development"

# The host the server will bind to. "0.0.0.0" makes it accessible on your local network.
HOST="0.0.0.0"

# The port the server will listen on.
PORT="3001"

# The minimum level of logs to be displayed.
# Recommended for dev: "debug". Recommended for prod: "info".
# Options: 'fatal', 'error', 'warn', 'info', 'debug', 'trace'
LOG_LEVEL="debug"

# The full URL of the deployed frontend application.
# This is critical for the CORS security policy to work.
CORS_ORIGIN="http://localhost:5173"

#---------------------------------
# SECTION 2: EXTERNAL SERVICES (Secrets)
#---------------------------------
# Get your API key from Google AI Studio: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=""

# Get your connection string from a MongoDB Atlas cluster.
MONGO_URI=""

# Get your API key and index name from the Pinecone dashboard.
PINECONE_API_KEY=""
PINECONE_INDEX_NAME="pst-translations"

#---------------------------------
# SECTION 3: APPLICATION TUNING (Optional)
#---------------------------------
# Allows you to easily swap Gemini models without changing code.
GEMINI_BLUEPRINT_MODEL="gemini-2.5-flash-latest"
GEMINI_TRANSLATION_MODEL="gemini-2.5-flash-latest"
GEMINI_SYNC_MODEL="gemini-2.5-pro-latest"

# Configuration for the Gemini API retry logic.
GEMINI_MAX_RETRIES="3"
GEMINI_BACKOFF_MS="1000"

# Configuration for the API rate limiter.
RATE_LIMIT_MAX="100"
RATE_LIMIT_WINDOW="1 minute"
